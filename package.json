{
  "name": "grantry",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "OAuth credential broker for AI agents (MCP-compatible)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "check": "npm run audit:provider-scopes && npm run audit:provider-request-coverage && npm run audit:generic-coverage && npm run audit:i18n-coverage && npm run audit:gtm-evidence && npm run build && DATABASE_URL=${DATABASE_URL:-postgresql://user:pass@localhost:5432/grantry} prisma validate",
    "audit:i18n-coverage": "tsx scripts/audit-i18n-coverage.ts",
    "audit:gtm-evidence": "tsx scripts/audit-gtm-evidence.ts",
    "audit:provider-scopes": "tsx scripts/audit-provider-scopes.ts",
    "audit:provider-request-coverage": "tsx scripts/audit-provider-request-coverage.ts",
    "audit:generic-coverage": "tsx scripts/audit-generic-coverage.ts",
    "test:connections": "node --import tsx --test tests/connections/*.test.ts",
    "start": "node scripts/start-with-maintenance.mjs",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:dedupe-connections": "node scripts/dedupe-connections.mjs",
    "db:backfill-tenants": "node scripts/backfill-tenants.mjs",
    "db:backfill-provider-credentials": "node scripts/backfill-provider-credentials.mjs",
    "db:disable-platform-oauth-app-credentials": "node scripts/disable-platform-oauth-app-credentials.mjs",
    "db:backfill-agent-connection-grants": "node scripts/backfill-agent-connection-grants.mjs",
    "user:reset-password": "node scripts/reset-password.mjs",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@prisma/client": "^5.22.0",
    "@scalar/hono-api-reference": "^0.11.12",
    "@types/node": "^22.10.0",
    "arctic": "^2.3.4",
    "better-auth": "^1.0.0",
    "fflate": "^0.8.2",
    "hono": "^4.6.10",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
