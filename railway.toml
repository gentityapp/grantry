[build]
builder = "NIXPACKS"

[deploy]
startCommand = "mkdir -p /data && npx prisma generate && npx prisma db push --accept-data-loss && node --import tsx/esm src/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
