[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma generate && npm run db:dedupe-connections && npx prisma db push && node --import tsx/esm src/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
